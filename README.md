## datas

- faceImageWeb : it is set when we capture the face image for recognition in login modal

  - when it gets set, its set as base64 data
  - when resetCapture is called in face recognition, we reset this data
  - its value gets shown in the preview window of the login modal
  - its value is used when doing face recognition
